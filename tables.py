import argparse

table_parser = argparse.ArgumentParser(
    description="Pick and choose which tables to export. (Won't affect performance)",
)


def tables(args):
    # print(args)
    print("hi from tables")
